To use or test the archetype, run install, then either just run archetype:generate and select it from the list, or run a variant of:

mvn archetype:generate -DarchetypeGroupId=org.wicketstuff.scala -DarchetypeArtifactId=wicketstuff-scala-archetype -DgroupId=com.mycompany -DartifactId=myproject -DarchetypeVersion=6.0-SNAPSHOT
